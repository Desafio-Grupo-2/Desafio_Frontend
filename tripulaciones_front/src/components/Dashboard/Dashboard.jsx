import React from 'react';
import Home from '../Home/Home';

export default function Dashboard() {
    // Para los conductores, renderizamos directamente la vista Home que monta el mapa
    return <Home />;
}
