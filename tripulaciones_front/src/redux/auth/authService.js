// src/redux/auth/authService.js

// Aquí podrías usar fetch o axios para interactuar con tu API
// Ejemplo de funciones futuras:
// - login(userData)
// - logout()
// - register(userData)

const authService = {
  // login: async (userData) => {},
  // logout: async () => {},
  // register: async (userData) => {},
};

export default authService;
